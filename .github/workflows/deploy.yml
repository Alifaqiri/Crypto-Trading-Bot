# این فایل موقتاً غیرفعال شده تا از خطای اجرا جلوگیری شود.
name: 🔧 Disabled Deployment

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No operation
        run: echo "Deployment temporarily disabled"
